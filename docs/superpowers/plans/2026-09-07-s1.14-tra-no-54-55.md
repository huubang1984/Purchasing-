# Kế hoạch S1.14 — Trả hai khoản nợ 54–55 (hai phần chênh của review lượt 5)

> **Ngày lập: 2026-09-07 (sau khi PR #10 merge, `39b7920`).** Hai khoản này là hai phần chênh mà lượt
> review thứ năm để lại có tên. Thứ tự 55 → 54: nợ 55 thêm một bảng và có thể thêm hàm/trigger, nợ 54
> GHIM danh sách hàm trigger nên phải đi sau khi không còn gì đổi.

## 0. Hai khoản, một dòng mỗi khoản

| Nợ | Khoảng trống đã đo (review H5) | Cách đóng |
|---|---|---|
| 55 | Oracle tồn tại tổ chức: bucket người gọi của tổ chức THẬT ở `otp_rate_limits` (có khoá ngoại), của tổ chức LẠ ở bộ nhớ — hai bộ đếm rời ⇒ mồi N lần vào một UUID giả rồi gửi UUID ứng viên là phân biệt được bằng MỘT lời gọi | migration `042`: bảng `caller_rate_limits` KHÔNG `org_id`, không khoá ngoại, khoá `HMAC(pepper, "LOGIN_CALLER_TOAN_CUC" ‖ route ‖ ip)` ⇒ tổ chức thật và lạ tăng CÙNG MỘT HÀNG; bucket bộ nhớ bị XOÁ (không còn việc); bộ dọn 5 phút xoá hàng cũ hơn hai cửa sổ |
| 54 | Danh sách ghim thân hàm trigger ở hardening VIẾT TAY: 48 hàm `RETURNS trigger` trong `public`, ghim 11; 21 trigger của `kiem_danh_tinh_theo_phien` (013) chưa ghim định nghĩa | một test ĐẦY ĐỦ: tập hàm trigger trong `public` = tập ghim ∪ tập loại trừ có LÝ DO (thêm một hàm trigger mới mà không khai là ĐỎ); ghim định nghĩa 21 trigger của 013 |

## 1. Thứ tự — mỗi khoản một commit

| # | Nợ | Số đo chịu lực |
|---|---|---|
| 14.1 | **55** | mồi N lần vào UUID lạ rồi gửi orgId THẬT ⇒ 429 (cùng bộ đếm) — RED thật với bản S1.13; hai UUID lạ xen kẽ vẫn cùng bucket; địa chỉ khác ⇒ 200; bộ đếm sống qua khởi động lại tiến trình (hàng nằm ở CSDL); bộ dọn xoá hàng cũ hai cửa sổ và GIỮ hàng trong cửa sổ |
| 14.2 | **54** | thêm một hàm `RETURNS trigger` vào một migration giả mà không khai ⇒ test ĐỎ; DROP một trong 21 trigger của 013 ⇒ `migrate()` khôi phục |
| 14.3 | docs | ADR-024, STATE mục 29, review an ninh lượt 6, evidence trên HEAD |
