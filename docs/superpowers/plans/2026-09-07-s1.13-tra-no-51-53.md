# Kế hoạch S1.13 — Trả ba khoản nợ 51–53 (phần chênh của review lượt 4)

> **Ngày lập: 2026-09-07 (sau khi PR #9 merge, `c5743bf`).** Ba khoản này là ba phần chênh mà lượt
> review thứ tư để lại có tên; không khoản nào bị chặn bởi tầng chưa có. Mỗi khoản một commit, mỗi
> khoản một đột biến RED thật. Thứ tự: 53 → 52 → 51 — 51 ghim THÂN các hàm trigger nên phải đi cuối,
> sau khi không còn gì đổi ở 039/040/041.

## 0. Ba khoản, một dòng mỗi khoản

| Nợ | Khoảng trống đã đo (review H4) | Cách đóng |
|---|---|---|
| 53 | `send` chạy TRONG giao dịch của job `LOGIN_LINK_SEND`: `send` xong mà kết cục không ghi được ⇒ email mang token bị rollback (link chết), rồi email thứ hai | `JobHandler` được TRẢ VỀ một việc sau commit; runner chạy nó SAU khi DONE đã commit, ngoài mọi giao dịch, có trần; thất bại ⇒ `AFTER_COMMIT_FAILED` tới `onJobFailure`, job vẫn DONE (at-most-once cho phần gửi — nói ra) |
| 52 | Không trần TOÀN TỔ CHỨC cho `/auth/link`; tổ chức LẠ không bị đếm (hai giao dịch lỗi mỗi lời gọi); `/guest/redeem`, `/guest/otp/verify` không có bucket theo người gọi | `orgLimit` trên route (bucket `LOGIN_CALLER` với khoá theo route, không theo IP); bucket TRONG BỘ NHỚ cho tổ chức lạ (không khoá ngoại nào để đếm ở CSDL) có trần kích thước; `callerLimit` cho hai route khách |
| 51 | Thân bốn hàm trigger mới (039, 040 ×2, 041) và máy trạng thái 040 chưa được hardening ghim — `CREATE OR REPLACE` sau triển khai làm chúng no-op và sống qua `migrate()` | Năm mục hardening theo khuôn `la_duong_ung_dung` (chỉ canh khi hàm đã tồn tại) + ghim định nghĩa sáu trigger (`pg_get_triggerdef`); test đồng bộ thân migration ↔ hardening cho cả năm; test trôi: thay thân thành no-op ⇒ `migrate()` khôi phục |

## 1. Thứ tự — mỗi bước một commit

| # | Nợ | Số đo chịu lực (mỗi cái có đối chứng) |
|---|---|---|
| 13.1 | **53** | (a) `packages/outbox`: handler trả hàm ⇒ hàm chạy SAU khi hàng job đã DONE và thấy được từ kết nối khác; hàm ném ⇒ job vẫn DONE + `AFTER_COMMIT_FAILED`; hàm treo ⇒ cùng lý do, có trần; handler ném ⇒ hàm không chạy. (b) `apps/api`: tại lúc `send` được gọi, token đã COMMIT (đếm từ pool khác); bộ gửi treo ⇒ job DONE, lý do `AFTER_COMMIT_FAILED`, không log nào mang token |
| 13.2 | **52** | (a) 300 địa chỉ KHÁC NHAU gọi `/auth/link` cùng tổ chức ⇒ lần 301 là 429 dù bucket theo IP còn trống; (b) tổ chức lạ: lần N+1 ⇒ 429 (bộ nhớ), tổ chức thật cùng IP không bị ảnh hưởng; bucket đầy ⇒ fail-closed; (c) `/guest/redeem` token sai 30 lần rồi 429 |
| 13.3 | **51** | thay thân `mfa_reset_kiem_chuyen_trang_thai` thành `RETURN NEW` và DROP một trigger ⇒ `migrate()` khôi phục cả thân lẫn trigger; đồng bộ năm thân migration ↔ hardening; đột biến: bỏ mục hardening (tệp tạm) ⇒ không khôi phục |
| 13.4 | docs | ADR-023 (việc sau commit của runner — hợp đồng gói đổi), STATE (51–53 gạch tại chỗ, mục 28), review an ninh lượt 5, evidence trên HEAD |
